import { View, Text } from "react-native";
import React from "react";

const TitleWrapper = () => {
  return (
    <View className="absolute w-full">
      <Text className="text-center text-black ">DashboardShopSales</Text>
    </View>
  );
};

export default TitleWrapper;
